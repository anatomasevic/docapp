import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'doctor-assistant-app-3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
